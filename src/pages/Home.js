import { HomeTitle, HomeView } from './Home.styled';

const Home = () => {
  return (
    <HomeView>
      <HomeTitle>welcome to tweet-cards!</HomeTitle>
    </HomeView>
  );
};

export default Home;
